#!/usr/bin/env python3

\"\"\"\nEvren Village - Initial Scene Setup Script\n\nRun this script to initialize the base Blender scene with:\n- Project collections structure\n- Terrain base mesh\n- Zone boundaries\n- Render settings\n- Camera & lighting\n\nUsage in Blender:\n    Scripting workspace → Open scripts/setup.py → Press Alt+P\n\nUsage in Terminal (headless):\n    blender --background evren-masterplan-base.blend --python scripts/setup.py\n\"\"\"\n\nimport bpy\nimport json\nfrom pathlib import Path\nfrom utils import (\n    log, log_success, log_error, log_warning,\n    create_evren_scene, create_plane, create_material,\n    assign_material, setup_camera, setup_lighting,\n    configure_render_settings, load_json,\n    PROJECT_ROOT, ASSET_PATH, VILLAGE_WIDTH, VILLAGE_HEIGHT\n)\n\n# ============================================================================\n# STEP 1: SCENE INITIALIZATION\n# ============================================================================\n\ndef initialize_base_scene():\n    \"\"\"Initialize master scene structure.\"\"\"\n    log(\"========================================\")\n    log(\"EVREN VILLAGE - BASE SCENE SETUP\")\n    log(\"========================================\")\n    \n    # Create master collection structure\n    master_col = create_evren_scene()\n    log_success(\"Master collection created\")\n    \n    # Set scene units\n    bpy.context.scene.unit_settings.length_unit = 'METERS'\n    bpy.context.scene.unit_settings.scale_length = 1.0\n    log_success(\"Scene units set to meters\")\n    \n    return master_col\n\n# ============================================================================\n# STEP 2: CREATE TERRAIN BASE\n# ============================================================================\n\ndef create_terrain_base():\n    \"\"\"Create base terrain plane with subdivision.\"\"\"\n    log(\"Creating terrain base...\")\n    \n    # Create large plane\n    terrain = create_plane(\n        name=\"Terrain\",\n        size=2,\n        location=(VILLAGE_WIDTH/2, VILLAGE_HEIGHT/2, 0)\n    )\n    \n    # Scale to village dimensions\n    terrain.scale = (VILLAGE_WIDTH/2, VILLAGE_HEIGHT/2, 1)\n    \n    # Add subdivision surface for detail\n    subdiv = terrain.modifiers.new(name=\"Subdivision\", type='SUBSURF')\n    subdiv.levels = 3\n    subdiv.render_levels = 5\n    \n    # Add material\n    terrain_mat = create_material(\n        \"Terrain_Base\",\n        color=(0.85, 0.82, 0.78, 1.0)  # Natural earth tone\n    )\n    assign_material(terrain, terrain_mat)\n    \n    # Move to Terrain collection\n    bpy.context.collection.objects.unlink(terrain)\n    terrain_col = bpy.data.collections[\"Terrain\"]\n    terrain_col.objects.link(terrain)\n    \n    log_success(f\"Terrain created: {VILLAGE_WIDTH}m × {VILLAGE_HEIGHT}m\")\n    return terrain\n\n# ============================================================================\n# STEP 3: CREATE ZONE BOUNDARIES\n# ============================================================================\n\ndef create_zone_boundaries():\n    \"\"\"Create visual zone boundary markers.\"\"\"\n    log(\"Creating zone boundaries...\")\n    \n    zones_data = {\n        \"Nova_NW\": {\n            \"color\": (0.93, 0.86, 0.88, 1.0),  # Pink-beige\n            \"position\": (150, 125, 0.1)\n        },\n        \"Aurelia_NE\": {\n            \"color\": (0.94, 0.90, 0.75, 1.0),  # Yellow-beige\n            \"position\": (350, 125, 0.1)\n        },\n        \"Haven_SW\": {\n            \"color\": (0.86, 0.92, 0.94, 1.0),  # Blue-beige\n            \"position\": (150, 325, 0.1)\n        },\n        \"Zenith_SE\": {\n            \"color\": (0.86, 0.92, 0.86, 1.0),  # Green-beige\n            \"position\": (350, 325, 0.1)\n        }\n    }\n    \n    zones_col = bpy.data.collections[\"Zones\"]\n    \n    for zone_name, zone_data in zones_data.items():\n        # Create zone plane\n        zone_plane = create_plane(\n            name=f\"Zone_{zone_name}\",\n            size=1.5,\n            location=zone_data[\"position\"]\n        )\n        \n        # Create material\n        zone_mat = create_material(zone_name, color=zone_data[\"color\"])\n        assign_material(zone_plane, zone_mat)\n        \n        # Move to Zones collection\n        bpy.context.collection.objects.unlink(zone_plane)\n        zones_col.objects.link(zone_plane)\n    \n    log_success(f\"Created {len(zones_data)} zone boundaries\")\n    return zones_data\n\n# ============================================================================\n# STEP 4: CREATE AURA CENTRE\n# ============================================================================\n\ndef create_aura_centre():\n    \"\"\"Create Aura centre placeholder with concentric circles.\"\"\"\n    log(\"Creating Aura centre...\")\n    \n    aura_col = bpy.data.collections[\"Aura Centre\"]\n    \n    # Center location\n    center = (VILLAGE_WIDTH/2, VILLAGE_HEIGHT/2, 0.2)\n    \n    # Aura radius zones\n    zones = [\n        (\"Zone_A_Core\", 70, (0.56, 0.74, 0.49, 0.8)),      # Green core\n        (\"Zone_B_Buffer\", 120, (0.66, 0.81, 0.60, 0.7)),   # Lighter green\n        (\"Zone_C_Paths\", 155, (0.78, 0.81, 0.71, 0.6))     # Pale green\n    ]\n    \n    for zone_name, radius, color in zones:\n        # Create circle curve\n        bpy.ops.curve.primitive_circle_add(\n            radius=radius,\n            location=center,\n            vertices=64\n        )\n        circle = bpy.context.active_object\n        circle.name = zone_name\n        \n        # Create material\n        mat = create_material(zone_name, color=color)\n        circle.data.materials.append(mat)\n        \n        # Move to Aura collection\n        bpy.context.collection.objects.unlink(circle)\n        aura_col.objects.link(circle)\n    \n    # Create Banyan tree placeholder\n    banyan = create_plane(\n        name=\"Banyan_Tree_Placeholder\",\n        size=0.5,\n        location=center\n    )\n    banyan_mat = create_material(\"Banyan\", color=(0.30, 0.40, 0.25, 1.0))\n    assign_material(banyan, banyan_mat)\n    \n    bpy.context.collection.objects.unlink(banyan)\n    aura_col.objects.link(banyan)\n    \n    log_success(f\"Aura centre created at {center}\")\n    return center\n\n# ============================================================================\n# STEP 5: SETUP LIGHTING & CAMERA\n# ============================================================================\n\ndef setup_default_lighting_camera():\n    \"\"\"Setup camera and lighting rig.\"\"\"\n    log(\"Setting up camera and lighting...\")\n    \n    # Camera position (aerial view)\n    cam_location = (VILLAGE_WIDTH/2, VILLAGE_HEIGHT/2 - 150, 300)\n    cam_target = (VILLAGE_WIDTH/2, VILLAGE_HEIGHT/2, 0)\n    \n    camera = setup_camera(location=cam_location, target=cam_target)\n    log_success(f\"Camera positioned at {cam_location}\")\n    \n    # Lighting\n    sun = setup_lighting(sun_energy=2.0, use_hdri=False)\n    log_success(\"Lighting setup complete\")\n    \n    return camera, sun\n\n# ============================================================================\n# STEP 6: CONFIGURE RENDER SETTINGS\n# ============================================================================\n\ndef setup_rendering():\n    \"\"\"Configure render engine and output.\"\"\"\n    log(\"Configuring render settings...\")\n    \n    configure_render_settings(\n        engine='EEVEE',  # Fast preview\n        samples=64,\n        resolution=(1920, 1080)\n    )\n    \n    # Also configure Cycles for later\n    bpy.context.scene.cycles.samples = 256\n    bpy.context.scene.cycles.denoising_use_gpu = True\n    \n    log_success(\"Render settings configured\")\n\n# ============================================================================\n# STEP 7: LOAD PROJECT DATA\n# ============================================================================\n\ndef load_project_specifications():\n    \"\"\"Load plot and facility data from JSON.\"\"\"\n    log(\"Loading project specifications...\")\n    \n    try:\n        # Load plot data\n        plot_data = load_json(str(ASSET_PATH / \"data\" / \"plot-coordinates.json\"))\n        log_success(f\"Loaded {plot_data.get('total_plots', 0)} plot specifications\")\n        \n        # Load facility data\n        facility_data = load_json(str(ASSET_PATH / \"data\" / \"facility-specs.json\"))\n        total_facilities = len(facility_data.get('facilities', {}))\n        log_success(f\"Loaded {total_facilities} facility specifications\")\n        \n        return plot_data, facility_data\n    except Exception as e:\n        log_error(f\"Failed to load project data: {e}\")\n        return {}, {}\n\n# ============================================================================\n# STEP 8: CREATE REFERENCE GRID\n# ============================================================================\n\ndef create_reference_grid():\n    \"\"\"Create measurement grid overlay.\"\"\"\n    log(\"Creating reference grid...\")\n    \n    # Create grid plane\n    grid = create_plane(\n        name=\"Reference_Grid\",\n        size=2,\n        location=(VILLAGE_WIDTH/2, VILLAGE_HEIGHT/2, 0.05)\n    )\n    \n    grid.scale = (VILLAGE_WIDTH/2, VILLAGE_HEIGHT/2, 1)\n    \n    # Create material with checker pattern (optional - visual only)\n    grid_mat = create_material(\"Grid_Reference\", color=(0.9, 0.9, 0.9, 0.3))\n    assign_material(grid, grid_mat)\n    \n    # Move to Zones collection\n    bpy.context.collection.objects.unlink(grid)\n    zones_col = bpy.data.collections[\"Zones\"]\n    zones_col.objects.link(grid)\n    \n    log_success(\"Reference grid created\")\n    return grid\n\n# ============================================================================\n# MAIN EXECUTION\n# ============================================================================\n\ndef main():\n    \"\"\"Execute complete setup sequence.\"\"\"\n    try:\n        # Step 1: Initialize scene\n        master_col = initialize_base_scene()\n        \n        # Step 2: Create terrain\n        terrain = create_terrain_base()\n        \n        # Step 3: Create zones\n        zones = create_zone_boundaries()\n        \n        # Step 4: Create Aura centre\n        aura_center = create_aura_centre()\n        \n        # Step 5: Setup camera & lighting\n        camera, sun = setup_default_lighting_camera()\n        \n        # Step 6: Configure rendering\n        setup_rendering()\n        \n        # Step 7: Load data\n        plot_data, facility_data = load_project_specifications()\n        \n        # Step 8: Create grid\n        grid = create_reference_grid()\n        \n        # Final summary\n        log(\"\\n\" + \"=\"*40)\n        log_success(\"SETUP COMPLETE!\")\n        log(\"=\"*40)\n        log(f\"Project: Evren Village Organic Master Plan\")\n        log(f\"Dimensions: {VILLAGE_WIDTH}m × {VILLAGE_HEIGHT}m\")\n        log(f\"Total Area: {VILLAGE_WIDTH * VILLAGE_HEIGHT:,} m²\")\n        log(f\"Zones: {len(zones)}\")\n        log(f\"Plots: {plot_data.get('total_plots', 'N/A')}\")\n        log(f\"Facilities: {len(facility_data.get('facilities', {}))}\")\n        log(\"\\nNext steps:\")\n        log(\"1. Open 01-terrain-roads/ for road network generation\")\n        log(\"2. Run road-generation.py script\")\n        log(\"3. Import plot data and create residential layouts\")\n        log(\"4. Add facility models from 04-facilities/\")\n        log(\"=\"*40 + \"\\n\")\n        \n        return True\n        \n    except Exception as e:\n        log_error(f\"Setup failed: {e}\")\n        import traceback\n        traceback.print_exc()\n        return False\n\n# ============================================================================\n# EXECUTE\n# ============================================================================\n\nif __name__ == \"__main__\":\n    success = main()\n    exit(0 if success else 1)\n